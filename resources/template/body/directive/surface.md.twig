{#- A plane is a ground to stand something on, which text has none of. Its
    label is what it was called. -#}
{%- autoescape false -%}
{%- if node.option('label') %}**{{ node.option('label')|md_escape }}**

{% endif -%}
{%- for child in node.children -%}
{{ renderNode(child)|md_tidy }}

{% endfor -%}
{%- endautoescape -%}
