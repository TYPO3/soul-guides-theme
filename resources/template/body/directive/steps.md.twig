{#- An instruction is read from the top, and the set counts its own stops —
    here as the ordered list Markdown counts with. -#}
{%- autoescape false -%}
{%- for child in node.children -%}
{{ renderNode(child)|md_indent(loop.index ~ '. ') }}
{% endfor %}
{% endautoescape %}