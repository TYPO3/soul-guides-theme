{#- A grid is how a set stands, which a file of text has no way to say.
    What it holds stands one after another, in the source order. -#}
{%- autoescape false -%}
{%- for child in node.children -%}
{{ renderNode(child)|md_tidy }}

{% endfor -%}
{%- endautoescape -%}
