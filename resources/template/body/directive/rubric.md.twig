{#- The one heading a reference uses that no contents lists. It is still a
    heading, and the deepest one, because nothing is nested under it. -#}
{%- autoescape false -%}
###### {{ renderNode(node.content)|md_line }}

{% endautoescape %}