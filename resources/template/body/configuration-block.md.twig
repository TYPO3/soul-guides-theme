{#- The same setting written in two languages. Both stand here, each under
    the label its tab carried — a reader of the file chooses as they read. -#}
{%- autoescape false -%}
{%- for tab in node.tabs -%}
**{{ tab.label|md_escape }}**

{{ renderNode(tab.content)|md_tidy }}

{% endfor -%}
{%- endautoescape -%}
