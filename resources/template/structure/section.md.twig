{#- A section is its heading and what follows it. The nesting is the level of
    the heading, which the title node carries. -#}
{%- autoescape false -%}
{%- for child in node.children -%}
{{ renderNode(child)|md_tidy }}

{% endfor -%}
{%- endautoescape -%}
