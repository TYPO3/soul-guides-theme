{#- A `.. meta::` field under the name the author gave it. A `description`
    is the front matter's own — see `Opening` — and stands once. -#}
{%- autoescape false -%}
{%- if node.key != 'description' %}{{ node.key }}: {{ node.value|md_yaml }}{% endif %}
{% endautoescape %}