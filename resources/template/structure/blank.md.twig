{#
    A node a Markdown file has no place for: what a page says *about* itself.

    An HTML page carries it in the head. A Markdown file has no head, and an
    author or a `:navigation-title:` printed here would read as the document's
    own first sentence.
#}
