{#
    A field about the page that says nothing to a reader of this file: where
    it stands in a menu, whether a search may index it, which shape the HTML
    page takes. The front matter carries what the document says about itself;
    this is what it says to the renderer.
#}
