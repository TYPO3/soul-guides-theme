{#- The word, and what it stands for in brackets. A title attribute is a
    tooltip, which a file of text has no way to draw and no pointer to draw
    it for. -#}
{%- autoescape false -%}{{ node.term|md_escape }}{% if node.definition %} ({{ node.definition|md_escape }}){% endif %}{%- endautoescape -%}