{#- A label the document wrote, kept so a reference into this page has
    something to land on. Markdown has no anchor of its own. A link with a
    fragment nothing answers loses the reader that follows it — a program —
    the passage it points at. -#}
{%- autoescape false -%}<a id="{{ node.value }}"></a>
{%- endautoescape -%}
