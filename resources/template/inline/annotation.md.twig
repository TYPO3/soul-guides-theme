{#- A footnote or a citation where the text refers to it. GFM writes both the
    same way, and the passage itself stands at the foot of the page. -#}
{%- autoescape false -%}[^{{ node.value }}]{%- endautoescape -%}