{#- A footnote or a citation where it is referred to. GFM writes both the
    same way, and the passage itself is written at the foot of the page. -#}
{%- autoescape false -%}[^{{ node.value }}]{%- endautoescape -%}