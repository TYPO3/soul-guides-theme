{#- A text role, by its own name — the same lookup the HTML does, so a project
    that writes a role writes a file for each format and nothing else. -#}
{%- autoescape false -%}
{%- include [('inline/textroles/' ~ node.type ~ '.md.twig'), 'inline/textroles/unknown.md.twig'] -%}
{%- endautoescape -%}